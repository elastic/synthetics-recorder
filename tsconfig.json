/**
  tsconfig for frontend. It is supposed to be `tsconfig.frontend.json`
  but react-scripts can only understand `tsconfig.json`(cannot specify tsconfig path)
*/

{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "module": "esnext",
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["common/helper/**/*", "src/**/*"]
}
