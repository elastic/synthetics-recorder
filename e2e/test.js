// const { journey, step } = require("@elastic/synthetics");
//
// module.exports = journey("complete a recording session", async window => {
//   step("inputs a website name", async () => {
//     // this won't work, because PW doesn't have access to electron's window
//     await window.page.fill(
//       '[aria-label="Enter a URL in this field as the starting point of a journey"]',
//       "https://www.elastic.co"
//     );
//   });
// });
//
it("runs", () => {});
